<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record model="ir.actions.act_window" id="action_membercard_wizard">
        <field name="name">New Member Card</field>
        <field name="res_model">membercard.new.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>



    <record model="ir.ui.view" id="beesdoo_partner_form_view">
        <field name="name">beesdoo.partner.form.view</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form" />
        <field name="arch" type="xml">

            <xpath expr="//notebook" position="inside">
                <page
                    string="Member Card"
                    attrs="{'invisible': ['|', ('customer', '=', False), ('eater', 'not in', ('worker_eater', 'eater'))]}"
                >
                    <group>
                        <field name="member_card_to_be_printed" />
                        <field name="last_printed" />
                    </group>

                    <group attrs="{'invisible': ['|', ('customer', '=', False)]}">

                        <separator string="Cards" />
                        <field string="Cards" name="member_card_ids">
                            <tree editable="bottom">
                                <field name="barcode" />
                                <field name="create_date" />
                                <field name="end_date" />
                                <field name="responsible_id" />
                                <field name="comment" />
                                <field name="valid" />
                            </tree>
                        </field>
                        <group>
                            <button
                                string="New Card"
                                name="%(action_membercard_wizard)d"
                                type="action"
                                class="btn btn-primary"
                            />
                        </group>
                    </group>
                </page>

            </xpath>

        </field>
    </record>

    <record id="view_beesdoo_res_partner_filter" model="ir.ui.view">
        <field name="name">res.partner.select</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_res_partner_filter" />
        <!-- todo check if possible to extend the domain rather than replace -->
        <field name="priority">100</field>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='name']">
                <field
                    name="name"
                    filter_domain="['|','|','|',('display_name','ilike',self),('ref','=',self),('email','ilike',self),('barcode','ilike',self)]"
                />
            </xpath>
        </field>
    </record>

</odoo>
